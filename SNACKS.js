class Snacks{
    constructor(){}

    display(){
        var button = createButton("Shoes");
        button.position(600,300);
        button.mousePressed(function(){
            button.hide();
        })
    }
}